# Dependencies for the VERCEL serverless deployment only.
#
# For local development use backend/requirements.txt instead - it includes
# scikit-learn, pandas, SciPy and OpenCV, which give you the trained irrigation
# model and faster image analysis.
#
# Why this list is trimmed: Vercel's Python functions are capped at 250 MB
# uncompressed. The full stack measures 457 MB, dominated by SciPy (109 MB),
# OpenCV (106 MB), pandas (63 MB), NumPy (51 MB) and scikit-learn (45 MB).
# Removing everything except NumPy brings this to roughly 100 MB.
#
# The cost is that the irrigation module falls back to the water-balance rule
# engine rather than the trained Random Forest. Every other feature - disease
# analysis, soil analysis, varieties, fertilizer, the assistant - is unaffected,
# because OpenCV was always optional (NumPy fallbacks exist) and pandas was only
# used to build the model's input frame.

fastapi==0.115.6
python-multipart==0.0.20

pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

SQLAlchemy==2.0.36
psycopg[binary]==3.2.3

PyJWT==2.10.1
bcrypt==4.2.1

numpy==2.2.1
Pillow==11.1.0

httpx==0.28.1
